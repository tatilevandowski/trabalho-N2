# src/frete_api.py

class FreteAPI:
    """
    Simulação de uma API de serviço externo para cálculo de frete.
    Será mockada nos testes (Item 4).
    """
    def calcular_frete(self, itens) -> float:
        """
        Recebe a lista de itens e retorna o valor do frete.
        Deve ser implementada para uso real, mas nos testes será mockada para retornar um valor fixo.
        """
        raise NotImplementedError("Método de API externa não implementado.")